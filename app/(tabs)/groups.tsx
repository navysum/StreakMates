import { Placeholder } from '@/components/Placeholder';
import { Screen } from '@/components/Screen';

export default function GroupsScreen() {
  return (
    <Screen title="Groups" eyebrow="Not built yet">
      <Placeholder
        phase="Phase 2"
        what="Create a group, share its six-character code, and join one from a friend. Phase 3 adds the board where everyone's shared habits sit side by side."
      />
    </Screen>
  );
}
